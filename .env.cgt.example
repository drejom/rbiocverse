# cgt.coh.org work VM deployment (direct HPC access)
# SSH config has direct access - no ProxyJump needed:
#   Host gemini-login1.coh.org
#     Cipher aes128-ctr
#     Compression no
#     ServerAliveInterval 60

COMPOSE_PROJECT_NAME=rbiocverse
MANAGER_VERSION=latest

# Local SSH directory
SSH_MOUNT_PATH=~/.ssh

# Local data directory
DATA_PATH=./data

# Default Docker network
NETWORK_NAME=default

# Direct port mapping
MANAGER_PORT=3000
IDE_PORT=8000

# HPC settings - use login1 for direct access
HPC_SSH_USER=domeally
GEMINI_SSH_HOST=gemini-login1.coh.org
APOLLO_SSH_HOST=ppxhpcacc01.coh.org
DEFAULT_HPC=gemini

# SLURM defaults
DEFAULT_CPUS=2
DEFAULT_MEM=40G
DEFAULT_TIME=12:00:00

# Authentication (generate with: openssl rand -base64 48)
JWT_SECRET=your-secret-key-at-least-32-characters

# Development logging
LOG_LEVEL=info
DEBUG_COMPONENTS=

# SMTP email notifications (optional)
# Set SMTP_HOST to enable — omit to disable (log-only mode)
# cgt.coh.org has a local postfix relay: use localhost:25, no auth needed
SMTP_HOST=localhost
SMTP_PORT=25
SMTP_FROM=rbiocverse@cgt.coh.org
# SMTP_USER=          # omit for unauthenticated relay
# SMTP_PASS=          # omit for unauthenticated relay
ADMIN_EMAIL=domeally@coh.org

# Match local user
PUID=1000
PGID=1000
TZ=America/Los_Angeles
